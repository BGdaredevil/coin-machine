1. run `npm install` in separate terminal for `./server`;
2. run `npm start` in separate terminal for `./server`;
3. run `npm install` in separate terminal for `./client`;
4. run `npm run dev` in separate terminal for `./client`;
